## Generated by dust (version 0.15.3) - do not edit
plague_stochastic_humans <- R6::R6Class(
  "dust",
  cloneable = FALSE,

  private = list(
    pars_ = NULL,
    pars_multi_ = NULL,
    index_ = NULL,
    info_ = NULL,
    n_threads_ = NULL,
    n_particles_ = NULL,
    n_particles_each_ = NULL,
    shape_ = NULL,
    ptr_ = NULL,
    gpu_config_ = NULL,
    ode_control_ = NULL,
    methods_ = NULL,
    param_ = list(season = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     a = list(has_default = TRUE, default_value = 0.004, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     beta_h = list(has_default = TRUE, default_value = 0.01, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     beta_r = list(has_default = TRUE, default_value = 4.7, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     d_f = list(has_default = TRUE, default_value = 10L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     d_h = list(has_default = TRUE, default_value = 0.04, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     d_r = list(has_default = TRUE, default_value = 0.2, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     dt = list(has_default = TRUE, default_value = list("/", 1L, 365L), rank = 0, min = -Inf, max = Inf, integer = FALSE),
     g_h = list(has_default = TRUE, default_value = 0.1, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     g_r = list(has_default = TRUE, default_value = 0.02, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     I_ini = list(has_default = TRUE, default_value = 10L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     K_f = list(has_default = TRUE, default_value = 6.57, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     K_h = list(has_default = TRUE, default_value = 5000L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     K_r = list(has_default = TRUE, default_value = 2500L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     m_h = list(has_default = TRUE, default_value = 26L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     m_r = list(has_default = TRUE, default_value = 20L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     p = list(has_default = TRUE, default_value = 0.975, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     r_f = list(has_default = TRUE, default_value = 20L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     r_h = list(has_default = TRUE, default_value = 0.045, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     r_r = list(has_default = TRUE, default_value = 5L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     S_ini = list(has_default = TRUE, default_value = 1L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     seasonal_amplitude = list(has_default = TRUE, default_value = 0.2, rank = 0, min = -Inf, max = Inf, integer = FALSE)),
    reload_ = NULL
  ),

  public = list(
    initialize = function(pars, time, n_particles, n_threads = 1L,
                          seed = NULL, pars_multi = FALSE,
                          deterministic = FALSE,
                          gpu_config = NULL, ode_control = NULL) {
      if (is.null(gpu_config)) {
        private$methods_ <- list(
           alloc = dust_cpu_plague_stochastic_humans_alloc,
           run = dust_cpu_plague_stochastic_humans_run,
           simulate = dust_cpu_plague_stochastic_humans_simulate,
           run_adjoint = dust_cpu_plague_stochastic_humans_run_adjoint,
           set_index = dust_cpu_plague_stochastic_humans_set_index,
           n_state = dust_cpu_plague_stochastic_humans_n_state,
           update_state = dust_cpu_plague_stochastic_humans_update_state,
           state = dust_cpu_plague_stochastic_humans_state,
           time = dust_cpu_plague_stochastic_humans_time,
           reorder = dust_cpu_plague_stochastic_humans_reorder,
           resample = dust_cpu_plague_stochastic_humans_resample,
           rng_state = dust_cpu_plague_stochastic_humans_rng_state,
           set_rng_state = dust_cpu_plague_stochastic_humans_set_rng_state,
           set_n_threads = dust_cpu_plague_stochastic_humans_set_n_threads,
           set_data = dust_cpu_plague_stochastic_humans_set_data,
           compare_data = dust_cpu_plague_stochastic_humans_compare_data,
           filter = dust_cpu_plague_stochastic_humans_filter,
           set_stochastic_schedule = dust_cpu_plague_stochastic_humans_set_stochastic_schedule,
           ode_statistics = dust_cpu_plague_stochastic_humans_ode_statistics)
      } else {
        private$methods_ <- list(alloc = function(...) {
          stop("GPU support not enabled for this object")
        })
      }
      res <- private$methods_$alloc(pars, pars_multi, time, n_particles,
                        n_threads, seed, deterministic, gpu_config, ode_control)
      private$pars_ <- pars
      private$pars_multi_ <- pars_multi
      private$n_threads_ <- n_threads
      private$ptr_ <- res[[1L]]
      private$info_ <- res[[2L]]
      private$shape_ <- res[[3L]]
      private$gpu_config_ <- res[[4L]]
      private$ode_control_ <- res[[5L]]
      private$n_particles_ <- prod(private$shape_)
      if (pars_multi) {
        private$n_particles_each_ <- private$n_particles_ / length(pars)
      } else {
        private$n_particles_each_ <- private$n_particles_
      }
    },

    name = function() {
      "plague_stochastic_humans"
    },

    param = function() {
      private$param_
    },

    run = function(time_end) {
      m <- private$methods_$run(private$ptr_, time_end)
      rownames(m) <- names(private$index_)
      m
    },

    simulate = function(time_end) {
      m <- private$methods_$simulate(private$ptr_, time_end)
      rownames(m) <- names(private$index_)
      m
    },

    run_adjoint = function() {
      res <- private$methods_$run_adjoint(private$ptr_)
      ## This approach may want to change, because it won't cope well
      ## with cases where are gradient is structured with respect to
      ## some parameters, but perhaps it's still a reasonable thing to
      ## do? While we have array-free models it's fine though.
      nms <- private$info_$adjoint
      if (!is.null(nms) && length(nms) == length(res$gradient)) {
        names(res$gradient) <- nms
      }
      res
    },

    set_index = function(index) {
      private$methods_$set_index(private$ptr_, index)
      private$index_ <- index
      invisible()
    },

    index = function() {
      private$index_
    },

    ode_control = function() {
      private$ode_control_
    },

    ode_statistics = function() {
      private$methods_$ode_statistics(private$ptr_)
    },

    n_threads = function() {
      private$n_threads_
    },

    n_state = function() {
      private$methods_$n_state(private$ptr_)
    },

    n_particles = function() {
      private$n_particles_
    },

    n_particles_each = function() {
      private$n_particles_each_
    },

    shape = function() {
      private$shape_
    },

    update_state = function(pars = NULL, state = NULL, time = NULL,
                            set_initial_state = NULL, index = NULL,
                            reset_step_size = NULL) {
      info <- private$methods_$update_state(private$ptr_, pars, state, time,
                                          set_initial_state, index,
                                          reset_step_size)
      if (!is.null(pars)) {
        private$info_ <- info
        private$pars_ <- pars
      }
      invisible()
    },

    state = function(index = NULL) {
      m <- private$methods_$state(private$ptr_, index)
      rownames(m) <- names(index)
      m
    },

    time = function() {
      private$methods_$time(private$ptr_)
    },

    set_stochastic_schedule = function(time) {
      private$methods_$set_stochastic_schedule(private$ptr_, time)
      invisible()
    },

    reorder = function(index) {
      storage.mode(index) <- "integer"
      private$methods_$reorder(private$ptr_, index)
      invisible()
    },

    resample = function(weights) {
      invisible(private$methods_$resample(private$ptr_, weights))
    },

    info = function() {
      private$info_
    },

    pars = function() {
      private$pars_
    },

    rng_state = function(first_only = FALSE, last_only = FALSE) {
      private$methods_$rng_state(private$ptr_, first_only, last_only)
    },

    set_rng_state = function(rng_state) {
      private$methods_$set_rng_state(private$ptr_, rng_state)
      invisible()
    },

    has_openmp = function() {
      dust_cpu_plague_stochastic_humans_capabilities()[["openmp"]]
    },

    has_gpu_support = function(fake_gpu = FALSE) {
      if (fake_gpu) {
        FALSE
      } else {
        dust_cpu_plague_stochastic_humans_capabilities()[["gpu"]]
      }
    },

    has_compare = function() {
      dust_cpu_plague_stochastic_humans_capabilities()[["compare"]]
    },

    real_size = function() {
      dust_cpu_plague_stochastic_humans_capabilities()[["real_size"]]
    },

    time_type = function() {
      dust_cpu_plague_stochastic_humans_capabilities()[["time_type"]]
    },

    rng_algorithm = function() {
      dust_cpu_plague_stochastic_humans_capabilities()[["rng_algorithm"]]
    },

    uses_gpu = function(fake_gpu = FALSE) {
      real_gpu <- private$gpu_config_$real_gpu
      !is.null(real_gpu) && (fake_gpu || real_gpu)
    },

    n_pars = function() {
      if (private$pars_multi_) length(private$pars_) else 0L
    },

    set_n_threads = function(n_threads) {
      prev <- private$n_threads_
      private$methods_$set_n_threads(private$ptr_, n_threads)
      private$n_threads_ <- n_threads
      invisible(prev)
    },

    set_data = function(data, shared = FALSE) {
      private$methods_$set_data(private$ptr_, data, shared)
    },

    compare_data = function() {
      private$methods_$compare_data(private$ptr_)
    },

    filter = function(time_end = NULL, save_trajectories = FALSE,
                      time_snapshot = NULL, min_log_likelihood = NULL) {
      private$methods_$filter(private$ptr_, time_end, save_trajectories,
                              time_snapshot, min_log_likelihood)
    },

    gpu_info = function() {
      ret <- dust_plague_stochastic_humans_gpu_info()
      ret$devices <- as.data.frame(ret$devices, stringsAsFactors = FALSE)
      parent <- parent.env(environment())
      if (ret$has_cuda && exists("private", parent, inherits = FALSE)) {
        ret$config <- private$gpu_config_
      }
      ret
    }
  ))
class(plague_stochastic_humans) <- c("dust_generator", class(plague_stochastic_humans))
plague_stochastic <- R6::R6Class(
  "dust",
  cloneable = FALSE,

  private = list(
    pars_ = NULL,
    pars_multi_ = NULL,
    index_ = NULL,
    info_ = NULL,
    n_threads_ = NULL,
    n_particles_ = NULL,
    n_particles_each_ = NULL,
    shape_ = NULL,
    ptr_ = NULL,
    gpu_config_ = NULL,
    ode_control_ = NULL,
    methods_ = NULL,
    param_ = list(contact = list(has_default = FALSE, default_value = NULL, rank = 2, min = -Inf, max = Inf, integer = FALSE),
     I_ini = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     season = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     a = list(has_default = TRUE, default_value = 0.004, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     beta_r = list(has_default = TRUE, default_value = 4.7, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     d_f = list(has_default = TRUE, default_value = 10L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     d_r = list(has_default = TRUE, default_value = 0.2, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     dt = list(has_default = TRUE, default_value = list("/", 1L, 365L), rank = 0, min = -Inf, max = Inf, integer = FALSE),
     g_r = list(has_default = TRUE, default_value = 0.02, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     K_f = list(has_default = TRUE, default_value = 6.57, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     K_r = list(has_default = TRUE, default_value = 2500L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     m_r = list(has_default = TRUE, default_value = 20L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     mu_f = list(has_default = TRUE, default_value = 0.008, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     mu_r = list(has_default = TRUE, default_value = 0.03, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     npop = list(has_default = TRUE, default_value = 25L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     p = list(has_default = TRUE, default_value = 0.975, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     r_f = list(has_default = TRUE, default_value = 20L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     r_r = list(has_default = TRUE, default_value = 5L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     S_ini = list(has_default = TRUE, default_value = 1L, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     seasonal_amplitude = list(has_default = TRUE, default_value = 0.2, rank = 0, min = -Inf, max = Inf, integer = FALSE)),
    reload_ = NULL
  ),

  public = list(
    initialize = function(pars, time, n_particles, n_threads = 1L,
                          seed = NULL, pars_multi = FALSE,
                          deterministic = FALSE,
                          gpu_config = NULL, ode_control = NULL) {
      if (is.null(gpu_config)) {
        private$methods_ <- list(
           alloc = dust_cpu_plague_stochastic_alloc,
           run = dust_cpu_plague_stochastic_run,
           simulate = dust_cpu_plague_stochastic_simulate,
           run_adjoint = dust_cpu_plague_stochastic_run_adjoint,
           set_index = dust_cpu_plague_stochastic_set_index,
           n_state = dust_cpu_plague_stochastic_n_state,
           update_state = dust_cpu_plague_stochastic_update_state,
           state = dust_cpu_plague_stochastic_state,
           time = dust_cpu_plague_stochastic_time,
           reorder = dust_cpu_plague_stochastic_reorder,
           resample = dust_cpu_plague_stochastic_resample,
           rng_state = dust_cpu_plague_stochastic_rng_state,
           set_rng_state = dust_cpu_plague_stochastic_set_rng_state,
           set_n_threads = dust_cpu_plague_stochastic_set_n_threads,
           set_data = dust_cpu_plague_stochastic_set_data,
           compare_data = dust_cpu_plague_stochastic_compare_data,
           filter = dust_cpu_plague_stochastic_filter,
           set_stochastic_schedule = dust_cpu_plague_stochastic_set_stochastic_schedule,
           ode_statistics = dust_cpu_plague_stochastic_ode_statistics)
      } else {
        private$methods_ <- list(alloc = function(...) {
          stop("GPU support not enabled for this object")
        })
      }
      res <- private$methods_$alloc(pars, pars_multi, time, n_particles,
                        n_threads, seed, deterministic, gpu_config, ode_control)
      private$pars_ <- pars
      private$pars_multi_ <- pars_multi
      private$n_threads_ <- n_threads
      private$ptr_ <- res[[1L]]
      private$info_ <- res[[2L]]
      private$shape_ <- res[[3L]]
      private$gpu_config_ <- res[[4L]]
      private$ode_control_ <- res[[5L]]
      private$n_particles_ <- prod(private$shape_)
      if (pars_multi) {
        private$n_particles_each_ <- private$n_particles_ / length(pars)
      } else {
        private$n_particles_each_ <- private$n_particles_
      }
    },

    name = function() {
      "plague_stochastic"
    },

    param = function() {
      private$param_
    },

    run = function(time_end) {
      m <- private$methods_$run(private$ptr_, time_end)
      rownames(m) <- names(private$index_)
      m
    },

    simulate = function(time_end) {
      m <- private$methods_$simulate(private$ptr_, time_end)
      rownames(m) <- names(private$index_)
      m
    },

    run_adjoint = function() {
      res <- private$methods_$run_adjoint(private$ptr_)
      ## This approach may want to change, because it won't cope well
      ## with cases where are gradient is structured with respect to
      ## some parameters, but perhaps it's still a reasonable thing to
      ## do? While we have array-free models it's fine though.
      nms <- private$info_$adjoint
      if (!is.null(nms) && length(nms) == length(res$gradient)) {
        names(res$gradient) <- nms
      }
      res
    },

    set_index = function(index) {
      private$methods_$set_index(private$ptr_, index)
      private$index_ <- index
      invisible()
    },

    index = function() {
      private$index_
    },

    ode_control = function() {
      private$ode_control_
    },

    ode_statistics = function() {
      private$methods_$ode_statistics(private$ptr_)
    },

    n_threads = function() {
      private$n_threads_
    },

    n_state = function() {
      private$methods_$n_state(private$ptr_)
    },

    n_particles = function() {
      private$n_particles_
    },

    n_particles_each = function() {
      private$n_particles_each_
    },

    shape = function() {
      private$shape_
    },

    update_state = function(pars = NULL, state = NULL, time = NULL,
                            set_initial_state = NULL, index = NULL,
                            reset_step_size = NULL) {
      info <- private$methods_$update_state(private$ptr_, pars, state, time,
                                          set_initial_state, index,
                                          reset_step_size)
      if (!is.null(pars)) {
        private$info_ <- info
        private$pars_ <- pars
      }
      invisible()
    },

    state = function(index = NULL) {
      m <- private$methods_$state(private$ptr_, index)
      rownames(m) <- names(index)
      m
    },

    time = function() {
      private$methods_$time(private$ptr_)
    },

    set_stochastic_schedule = function(time) {
      private$methods_$set_stochastic_schedule(private$ptr_, time)
      invisible()
    },

    reorder = function(index) {
      storage.mode(index) <- "integer"
      private$methods_$reorder(private$ptr_, index)
      invisible()
    },

    resample = function(weights) {
      invisible(private$methods_$resample(private$ptr_, weights))
    },

    info = function() {
      private$info_
    },

    pars = function() {
      private$pars_
    },

    rng_state = function(first_only = FALSE, last_only = FALSE) {
      private$methods_$rng_state(private$ptr_, first_only, last_only)
    },

    set_rng_state = function(rng_state) {
      private$methods_$set_rng_state(private$ptr_, rng_state)
      invisible()
    },

    has_openmp = function() {
      dust_cpu_plague_stochastic_capabilities()[["openmp"]]
    },

    has_gpu_support = function(fake_gpu = FALSE) {
      if (fake_gpu) {
        FALSE
      } else {
        dust_cpu_plague_stochastic_capabilities()[["gpu"]]
      }
    },

    has_compare = function() {
      dust_cpu_plague_stochastic_capabilities()[["compare"]]
    },

    real_size = function() {
      dust_cpu_plague_stochastic_capabilities()[["real_size"]]
    },

    time_type = function() {
      dust_cpu_plague_stochastic_capabilities()[["time_type"]]
    },

    rng_algorithm = function() {
      dust_cpu_plague_stochastic_capabilities()[["rng_algorithm"]]
    },

    uses_gpu = function(fake_gpu = FALSE) {
      real_gpu <- private$gpu_config_$real_gpu
      !is.null(real_gpu) && (fake_gpu || real_gpu)
    },

    n_pars = function() {
      if (private$pars_multi_) length(private$pars_) else 0L
    },

    set_n_threads = function(n_threads) {
      prev <- private$n_threads_
      private$methods_$set_n_threads(private$ptr_, n_threads)
      private$n_threads_ <- n_threads
      invisible(prev)
    },

    set_data = function(data, shared = FALSE) {
      private$methods_$set_data(private$ptr_, data, shared)
    },

    compare_data = function() {
      private$methods_$compare_data(private$ptr_)
    },

    filter = function(time_end = NULL, save_trajectories = FALSE,
                      time_snapshot = NULL, min_log_likelihood = NULL) {
      private$methods_$filter(private$ptr_, time_end, save_trajectories,
                              time_snapshot, min_log_likelihood)
    },

    gpu_info = function() {
      ret <- dust_plague_stochastic_gpu_info()
      ret$devices <- as.data.frame(ret$devices, stringsAsFactors = FALSE)
      parent <- parent.env(environment())
      if (ret$has_cuda && exists("private", parent, inherits = FALSE)) {
        ret$config <- private$gpu_config_
      }
      ret
    }
  ))
class(plague_stochastic) <- c("dust_generator", class(plague_stochastic))
## Generated by odin.dust (version 0.3.13) - do not edit
plague_stochastic_humans$set("public", "transform_variables", function(y) {
    info <- self$info()
    set_dim <- function(x, dimx) {
        if (length(dimx) > 1L) {
            dim(x) <- dimx
        }
        x
    }
    if (is.matrix(y)) {
        n <- ncol(y)
        Map(function(i, d) set_dim(y[i, , drop = FALSE], c(d,
            n)), info$index, info$dim)
    } else if (is.array(y)) {
        n <- dim(y)[2:3]
        Map(function(i, d) set_dim(y[i, , , drop = FALSE], c(d,
            n)), info$index, info$dim)
    } else {
        Map(function(i, d) set_dim(y[i], d), info$index, info$dim)
    }
})
plague_stochastic$set("public", "transform_variables", function(y) {
    info <- self$info()
    set_dim <- function(x, dimx) {
        if (length(dimx) > 1L) {
            dim(x) <- dimx
        }
        x
    }
    if (is.matrix(y)) {
        n <- ncol(y)
        Map(function(i, d) set_dim(y[i, , drop = FALSE], c(d,
            n)), info$index, info$dim)
    } else if (is.array(y)) {
        n <- dim(y)[2:3]
        Map(function(i, d) set_dim(y[i, , , drop = FALSE], c(d,
            n)), info$index, info$dim)
    } else {
        Map(function(i, d) set_dim(y[i], d), info$index, info$dim)
    }
})

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monty_fit.R
\name{plague_fit_fixed_pars}
\alias{plague_fit_fixed_pars}
\title{Build the fixed (non-fitted) parameter list for a plague humans fit.}
\usage{
plague_fit_fixed_pars(
  scenario = "historical",
  fitted_names = plague_fit_fitted_names()
)
}
\arguments{
\item{scenario}{Scenario name, path to YAML, or parameter list. Defaults
to \code{"historical"} — the scenario the monty fit pipeline was calibrated
against.}

\item{fitted_names}{Names of parameters that the sampler will set; these
are stripped from the fixed set. Defaults to \code{\link[=plague_fit_fitted_names]{plague_fit_fitted_names()}}.}
}
\value{
Plain named list of fixed parameters.
}
\description{
Loads a scenario via \code{\link[=load_scenario]{load_scenario()}}, drops any parameter that the
sampler will vary, and filters to just the keys the odin2 humans model
accepts. The returned list is suitable for the \code{fixed} argument of
\code{monty::monty_packer()} or as \code{pars} for \code{dust2::dust_system_create()}.
}

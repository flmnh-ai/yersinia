% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_model_config.R
\name{model_config_default}
\alias{model_config_default}
\title{Default model configuration for a fresh Virtual Lab session.}
\usage{
model_config_default(scenario = "defaults")
}
\arguments{
\item{scenario}{Base scenario name. Default \code{"defaults"} — the curated
baseline scenario designed for out-of-the-box outbreak fitting. Era-
specific scenarios (\code{"historical"}, \code{"modern-estimates"},
\code{"didelot"}) are available for paper reproduction.}
}
\value{
A model-config list.
}
\description{
Mirrors the Barcelona / Cairo vignette fitting workflows: biology +
likelihood-overdispersion are shared across the cohort; transmission to
humans and ignition + record-quality knobs are local (per outbreak).
Populations (\code{K_h}, \code{K_r}) are \emph{not} in the fitted set — when omitted,
\code{\link[=lab_fit_assemble]{lab_fit_assemble()}} pins them per-group at each outbreak's known
\code{population} from the \link{outbreaks} dataset. Same applies to \code{g_h} — the
vignettes pin it at the scenario value because it's only weakly
identifiable from death counts alone.
}
\details{
\code{rho} (flea redistribution exponent) is also intentionally \emph{not} fitted
by default: it's confounded with \code{beta_r} via the rat-side R₀ formula
\verb{R₀ ∝ beta_r · (1 − e^(−rho)) / delta_R}, so jointly fitting both
creates a ridge in the posterior that wrecks rhat. Pin rho at the
scenario value and let beta_r carry the rat-side identification.

To fit K_h/K_r, g_h, or rho, add them via the Model tab.
}

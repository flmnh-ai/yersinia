% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_fit.R
\name{lab_fit_run_production}
\alias{lab_fit_run_production}
\title{Run a stochastic production fit on top of a deterministic pilot.}
\usage{
lab_fit_run_production(
  pilot_setup,
  pilot_samples,
  n_chains = 4L,
  n_iter = 5000L,
  burnin_frac = 0.2,
  n_particles = 500L,
  n_threads = 2L,
  vcv_inflation = NULL
)
}
\arguments{
\item{pilot_setup}{The setup list returned by \code{\link[=lab_fit_assemble]{lab_fit_assemble()}}.}

\item{pilot_samples}{The samples returned by \code{\link[=lab_fit_run]{lab_fit_run()}}.}

\item{n_chains}{Number of chains. Default 4.}

\item{n_iter}{Iterations per chain. Default 5000 (much shorter than
pilot since the proposal is already tuned).}

\item{burnin_frac}{Fraction to discard. Default 0.2 (chains start
from a converged pilot region, so minimal warmup needed).}

\item{n_particles}{Particle filter size. Default 500.}

\item{n_threads}{OpenMP threads. Default 2.}

\item{vcv_inflation}{Factor to scale the tuned pilot VCV by. Default
\verb{0.7 · (2.38² / n_pars)} (Roberts–Rosenthal optimal-scaling, slightly
shrunk to give the stochastic chain cushion against filter noise).}
}
\value{
A \code{monty_samples} object with the warmup iterations removed.
}
\description{
After a pilot completes, use the adaptive sampler's tuned proposal VCV
to warm-start a shorter stochastic chain that respects the actual
particle-filter likelihood (the pilot's deterministic unfilter is an
expected-value approximation). Returns the production samples; the
caller is responsible for keeping the pilot around for diagnostics.
}
\details{
Mirrors the two-stage workflow in
\code{vignettes/monty-barcelona-hierarchical.qmd} and friends.
}

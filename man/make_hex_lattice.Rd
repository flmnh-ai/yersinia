% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ca.R
\name{make_hex_lattice}
\alias{make_hex_lattice}
\title{Build a hexagonal CA lattice}
\usage{
make_hex_lattice(side)
}
\arguments{
\item{side}{Numeric. Side length of the square bounding box. The number
of cells produced is approximately \code{side^2}.}
}
\value{
A list with \code{neighbors}, \code{A}, \code{npop}, and \code{sf} (an \code{sf} data frame
with \code{cell_id} and \code{geometry} columns).
}
\description{
Constructs a hex-tessellated grid covering a \verb{side × side} bounding box
(one hex cell per unit area, pointy-top by default), with \strong{6-neighbour}
adjacency. Requires the \code{sf} package. Returns the same \code{lattice} interface
as \code{\link[=make_square_grid]{make_square_grid()}} plus an \code{sf} field that lets you visualize results
with \code{ggplot2::geom_sf()}.
}
\seealso{
\code{\link[=make_square_grid]{make_square_grid()}} for the square variant.
}

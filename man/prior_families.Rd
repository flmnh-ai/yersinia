% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_priors.R
\name{prior_families}
\alias{prior_families}
\title{Registry of supported prior distribution families.}
\usage{
prior_families()
}
\value{
Named list of family definitions.
}
\description{
Each entry is a list with:
\itemize{
\item \code{params}: named list of parameter specs \verb{(default, min, max, label)},
in the order the family expects them when called via the monty DSL
\item \code{density}: function \verb{(x, p)} returning the PDF at \code{x}, where \code{p} is a
named list of parameter values
\item \code{range}: function \code{(p)} returning a length-2 numeric \code{c(low, high)} —
sensible plot range for the family at parameter values \code{p}
}
}
\details{
v1 supports six families: \code{Uniform}, \code{Normal}, \code{LogNormal}, \code{Gamma},
\code{Exponential}, \code{Beta}. Names match the monty DSL exactly.
}

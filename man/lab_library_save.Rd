% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_library.R
\name{lab_library_save}
\alias{lab_library_save}
\title{Save the current session to the fit library.}
\usage{
lab_library_save(lab_session, name, library_dir = NULL, autosaved = FALSE)
}
\arguments{
\item{lab_session}{A \link{LabSession} instance.}

\item{name}{Human-readable name for this fit. Used to derive the filename.}

\item{library_dir}{Directory to write to. Defaults to \code{\link[=lab_library_dir]{lab_library_dir()}}.}

\item{autosaved}{Mark this entry as automatically saved. Autosaves are
flagged in the metadata so the app can filter them out of the picker,
and are never overwritten.}
}
\value{
Invisibly, the metadata list that was written.
}
\description{
Writes \code{lab_session$to_list()} plus a metadata sidecar.
}
\details{
Two collision policies, because manual and automatic saves want opposite
things. A \emph{manual} save overwrites any existing entry with the same slug,
so iterating on one named fit updates it in place instead of accumulating
near-duplicates. An \emph{autosave} (\code{autosaved = TRUE}) never overwrites: its
generated name already carries a timestamp, and a numeric suffix is added
in the unlikely event two runs land in the same second. Every run is
therefore recoverable.
}

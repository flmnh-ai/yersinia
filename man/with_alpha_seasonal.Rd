% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/packer_helpers.R
\name{with_alpha_seasonal}
\alias{with_alpha_seasonal}
\title{Modulate per-group seasonal forcing by a fitted alpha exponent.}
\usage{
with_alpha_seasonal(packer, group_seasonal)
}
\arguments{
\item{packer}{A grouped packer or wrapper around one. Must have \code{alpha} as
a shared scalar parameter.}

\item{group_seasonal}{Named list keyed by group; each entry is a numeric
vector of per-day seasonal multipliers (length = number of simulation
days for that outbreak).}
}
\value{
A packer with wrapped \verb{$unpack}.
}
\description{
Replaces each group's \code{seasonal} vector with \code{group_seasonal[[g]] ^ alpha}.
Lets the data inform the strength of seasonality — \code{alpha = 0} collapses
seasonal to flat 1, \code{alpha = 1} is the raw climate signal, intermediate
values dampen, larger values amplify. Removes \code{alpha} from unpacked pars.
}

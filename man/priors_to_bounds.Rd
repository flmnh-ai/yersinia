% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_priors.R
\name{priors_to_bounds}
\alias{priors_to_bounds}
\title{Bounds for every parameter in a packer, including per-group decorations.}
\usage{
priors_to_bounds(priors, packer_names)
}
\arguments{
\item{priors}{Named list of priors.}

\item{packer_names}{Character vector of (possibly decorated) parameter
names from \code{packer$names()}.}
}
\value{
Named list of \code{c(low, high)} keyed by \code{packer_names}.
}
\description{
Maps each \code{packer_names} entry back to its base parameter (stripping any
\verb{<group>} decoration), looks up that parameter's prior, and returns the
bounds via \code{\link[=prior_bounds]{prior_bounds()}}. Suitable for passing to
\code{\link[=diagnose_bound_piling]{diagnose_bound_piling()}}.
}

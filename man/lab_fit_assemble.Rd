% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_fit.R
\name{lab_fit_assemble}
\alias{lab_fit_assemble}
\title{Assemble a deterministic-pilot fit from a LabSession.}
\usage{
lab_fit_assemble(lab_session, data = NULL)
}
\arguments{
\item{lab_session}{A \link{LabSession} whose \code{cohort_ids}, \code{model_config},
and \code{priors} fields are populated.}

\item{data}{Long outbreaks tibble (defaults to bundled \link{outbreaks}).}
}
\value{
Named list: \code{posterior}, \code{sampler}, \code{packer}, \code{unfilter},
\code{fixed_pars}, \code{data}, \code{cohort_ids}, \code{fitted_names}.
}
\description{
Returns a list containing the posterior model, sampler, packer, fixed
parameters, and metadata. Pass to \code{\link[=lab_fit_run]{lab_fit_run()}} (or call
\code{monty::monty_sample()} directly with \code{posterior} + \code{sampler}).
}
\details{
Branches on \code{length(cohort_ids)}:
\itemize{
\item \code{1}: standard single-outbreak setup, plain \code{monty_packer}.
\item \verb{>1}: grouped fit with \code{\link[monty:monty_packer_grouped]{monty::monty_packer_grouped()}} and
\code{\link[=with_per_group_fixed]{with_per_group_fixed()}} splicing per-outbreak populations into K_h
and K_r when those aren't in the fitted set.
}

Smoke-tests the unfilter at the prior medians so misconfigured fits
fail at assembly time, not deep inside \code{monty_sample()}.
}

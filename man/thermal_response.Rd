% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thermal.R
\name{thermal_response}
\alias{thermal_response}
\title{Thermal response on transmission (Brière, visible coordinates)}
\usage{
thermal_response(temperature, T_opt, hw_cold, hw_hot, floor = 1e-08)
}
\arguments{
\item{temperature}{Numeric vector of temperatures (degrees Celsius).}

\item{T_opt}{Temperature at which transmission peaks.}

\item{hw_cold}{Degrees from \code{T_opt} down to where transmission reaches zero.}

\item{hw_hot}{Degrees from \code{T_opt} up to where transmission reaches zero.}

\item{floor}{Smallest value returned, applied outside the zeros and for
degenerate parameters, so a bad MCMC proposal lowers the likelihood
instead of producing zero transmission or an error. Default 1e-8.}
}
\value{
Numeric vector in \verb{[floor, 1]}, one per input temperature.
}
\description{
Returns a per-temperature multiplier on transmission, equal to 1 at
\code{T_opt} and falling to zero at \code{T_opt - hw_cold} and \code{T_opt + hw_hot}.
This is simplified Brière-2 reparameterised so the peak and the distances
to the two zeros are the parameters:
}
\details{
\deqn{w(x) = (1 + x / h_c) (1 - x / h_h)^{h_h / h_c}, \quad x = T - T_{opt}}

Equivalent textbook coordinates: \code{T_min = T_opt - hw_cold},
\code{T_max = T_opt + hw_hot}, exponent \code{q = hw_hot / hw_cold}.
}

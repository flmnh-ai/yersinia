% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_library.R
\name{lab_library_load}
\alias{lab_library_load}
\title{Load a saved fit into a live session.}
\usage{
lab_library_load(lab_session, slug, library_dir = NULL)
}
\arguments{
\item{lab_session}{A \link{LabSession} to mutate in place.}

\item{slug}{Slug of the saved fit (from \code{\link[=lab_library_list]{lab_library_list()}}).}

\item{library_dir}{Directory to read from. Defaults to \code{\link[=lab_library_dir]{lab_library_dir()}}.}
}
\value{
Invisibly, \code{lab_session}.
}
\description{
Restores cohort, model config, priors, and the stored fit, then rebuilds
the dust2 setup that was deliberately not serialised. If the rebuild fails
(e.g. the bundled outbreak data changed under the saved cohort), the rest
of the state is still applied and \code{setup} is left \code{NULL} — the fit is
viewable, it just can't be promoted to production without a refit.
}

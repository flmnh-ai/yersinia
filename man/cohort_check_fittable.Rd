% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_fit.R
\name{cohort_check_fittable}
\alias{cohort_check_fittable}
\title{Stop early when a cohort contains outbreaks that cannot be fitted.}
\usage{
cohort_check_fittable(cohort_ids, data = NULL)
}
\arguments{
\item{cohort_ids}{Character vector of \code{outbreak_id}s.}

\item{data}{Long outbreaks tibble (defaults to bundled \link{outbreaks}).}
}
\value{
\code{cohort_ids}, invisibly, when every outbreak is fittable.
}
\description{
Most of Krauer's catalogue is not fittable: 78 of the 130 records lack a
population (which \code{lab_fit_assemble()} pins \code{K_h} and \code{K_r} to), a
whole-day reporting window (which the \code{D_h} accumulator resets on), or
both. The explorer greys those out rather than offering them, so reaching
this error means an id arrived from a saved session or a script. Failing
here, naming the outbreak and the reason, beats an \code{NA} propagating into
dust2 and surfacing as an inscrutable sampler error.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_compare.R
\name{lab_compare_trajectories}
\alias{lab_compare_trajectories}
\title{Posterior predictive trajectories for several saved fits.}
\usage{
lab_compare_trajectories(
  slugs,
  library_dir = NULL,
  n_draws = 40L,
  stages = NULL
)
}
\arguments{
\item{slugs}{Character vector of library slugs.}

\item{library_dir}{Library directory. Defaults to \code{\link[=lab_library_dir]{lab_library_dir()}}.}

\item{n_draws}{Posterior draws per fit. Default 40 — lower than the hero's,
since several fans share one panel.}

\item{stages}{Optional parallel vector of \code{"canonical"} / \code{"pilot"}.}
}
\value{
Long data frame with \code{fit}, \code{draw}, \code{group}, \code{time}, \code{mu}, carrying
a \code{skipped} attribute (character vector of labels).
}
\description{
Rebuilds each entry's dust2 setup (not serialised) and forward-simulates.
Entries whose setup cannot be rebuilt are skipped; their labels come back in
the \code{skipped} attribute so the caller can say which and why.
}

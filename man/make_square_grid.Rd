% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ca.R
\name{make_square_grid}
\alias{make_square_grid}
\title{Build a square-lattice CA grid}
\usage{
make_square_grid(n_row, n_col)
}
\arguments{
\item{n_row, n_col}{Integer dimensions of the lattice.}
}
\value{
A list with elements:
\itemize{
\item \code{neighbors} — list of integer vectors, one per cell
\item \code{A} — sparse \code{Matrix::sparseMatrix} adjacency, dims \verb{(npop, npop)}
\item \code{n_row}, \code{n_col}, \code{npop}
\item \code{cell_xy} — data frame with \code{row} and \code{col} columns
}
}
\description{
Constructs a \verb{n_row × n_col} regular grid with \strong{rook (4-neighbour)}
adjacency, returning a list with the neighbour list, sparse adjacency
matrix, and per-cell \verb{(row, col)} coordinates. Boundary cells have 2-3
neighbours; interior cells have 4. The neighbour ordering is row-major
(cell index = \code{(row - 1) * n_col + col}).
}
\seealso{
\code{\link[=make_hex_lattice]{make_hex_lattice()}} for the hex variant; \code{\link[=run_ca]{run_ca()}} consumes
the returned list directly.
}

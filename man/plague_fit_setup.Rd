% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monty_fit.R
\name{plague_fit_setup}
\alias{plague_fit_setup}
\title{Assemble everything needed to call \code{\link[monty:monty_sample]{monty::monty_sample()}} on a plague fit.}
\usage{
plague_fit_setup(
  data,
  scenario = "historical",
  fitted_names = plague_fit_fitted_names(),
  n_particles = 2000,
  n_threads = 2,
  prior = NULL,
  vcv = NULL,
  smoke_test = TRUE
)
}
\arguments{
\item{data}{Data frame with columns \code{time} and \code{deaths}.}

\item{scenario}{Fixed-parameter scenario (name, path, or list). Default
\code{"historical"}.}

\item{fitted_names}{Parameters for the sampler to vary. Default
\code{\link[=plague_fit_fitted_names]{plague_fit_fitted_names()}}.}

\item{n_particles}{Particle filter size. Default 2000.}

\item{n_threads}{OpenMP threads for the filter. Default 2.}

\item{prior}{monty model; default \code{\link[=plague_fit_prior]{plague_fit_prior()}}.}

\item{vcv}{Sampler VCV; default \code{\link[=plague_fit_vcv]{plague_fit_vcv()}}.}

\item{smoke_test}{If \code{TRUE} (default), call \code{dust_likelihood_run()} once
to validate filter + fixed params + data before returning.}
}
\value{
Named list: \code{posterior}, \code{sampler}, \code{filter}, \code{packer},
\code{fixed_pars}, \code{fitted_names}. The caller chooses \code{runner}, \code{n_steps},
\code{n_chains}, \code{burnin} when invoking \code{monty_sample()}.
}
\description{
Wires the filter, packer (with fixed parameters), likelihood, prior, and
sampler together. Runs the filter once at the fixed defaults as a smoke
test — if that throws, the parameters and data don't agree and you want
the error now rather than inside \code{monty_sample()}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/packer_helpers.R
\name{with_per_group_fixed}
\alias{with_per_group_fixed}
\title{Splice per-group fixed parameters into a grouped packer.}
\usage{
with_per_group_fixed(packer, group_fixed)
}
\arguments{
\item{packer}{A grouped packer (from \code{\link[monty:monty_packer_grouped]{monty::monty_packer_grouped()}}) or a
wrapper around one.}

\item{group_fixed}{Named list keyed by group; each entry is a named list of
parameters to splice into that group's unpacked pars. Groups not in
\code{group_fixed} are passed through unchanged.}
}
\value{
A packer with wrapped \verb{$unpack}. Class preserved so dust2's grouped
filter accepts it.
}
\description{
For values that are \emph{fixed but vary per group} (e.g. each outbreak's K_h,
K_r pinned to its Krauer-listed population), \code{\link[monty:monty_packer_grouped]{monty::monty_packer_grouped()}}'s
\code{fixed} argument doesn't help — it's global. This wrapper merges
\code{group_fixed[[g]]} into each group's pars during unpack.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ca.R
\name{ca_step}
\alias{ca_step}
\title{Advance the CA one tick}
\usage{
ca_step(
  state,
  sus,
  A,
  lookups,
  d_per_tick,
  P_recovery_sus = 20,
  grow_per_tick = 12
)
}
\arguments{
\item{state}{Integer vector of length \code{npop}, values \code{0}/\code{1}/\code{2}.}

\item{sus}{Numeric vector of length \code{npop}, values in \verb{[0, 100]}.}

\item{A}{Sparse adjacency matrix from \verb{make_*_grid()}.}

\item{lookups}{List of three functions (\code{T_E}, \code{T_P}, \code{Q}) of
\verb{S_frac ∈ [0, 1]}. Use \code{\link[=make_lookups]{make_lookups()}}.}

\item{d_per_tick}{Probability per tick that an isolated \code{P} cell decays
back to \code{S}. Estimate from your metapop using a long-run \code{P}-state
survival simulation.}

\item{P_recovery_sus}{Susceptibility (0-100) that recovered \code{P} cells
land at. Default \code{20} is just below the band where transmission becomes
reliable in the vignette's parameter regime.}

\item{grow_per_tick}{Increment in \code{sus} per tick for \code{S} cells.}
}
\value{
A list with the updated \code{state} and \code{sus} vectors.
}
\description{
Vectorized one-step update of a 3-state CA (\code{0} = susceptible, \code{1} =
epidemic, \code{2} = endemic) following K&G (2000) Table 3 semantics:
}
\details{
\itemize{
\item \verb{S → E} or \verb{S → P} if any neighbour is in \code{E} or \code{P}, with per-target
probability \verb{1 - (1 - T_E)^n_E × (1 - T_P)^n_P} and outcome \code{P} with
conditional probability \code{Q} (else \code{E}).
\item \verb{E → S_0} (deterministic — epidemics resolve in one tick).
\item \verb{P → S_w} with probability \code{d_per_tick}, sus reset to \code{P_recovery_sus}.
\item Susceptibility regrows in \code{S} cells by \code{grow_per_tick} (capped at 100).
}

All transitions are computed from the start-of-tick state; no
within-tick asynchronous updates.
}

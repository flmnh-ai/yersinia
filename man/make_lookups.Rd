% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ca.R
\name{make_lookups}
\alias{make_lookups}
\title{Build CA transition-probability lookups}
\usage{
make_lookups(TQ, mu_r_use)
}
\arguments{
\item{TQ}{Data frame with columns \code{source} (\code{"E"} or \code{"P"}), \code{mu_r},
\code{S_frac}, \code{T_i}, \code{Q_i}. The vignette produces this as \code{tq} or \code{ca_TQ}.}

\item{mu_r_use}{Coupling level at which to extract lookups (must match a
value in \code{TQ$mu_r}).}
}
\value{
A list of \verb{function(S_frac)} interpolators: \code{T_E}, \code{T_P}, \code{Q}.
}
\description{
From a tidy data frame of empirical \code{T_i} / \code{Q_i} estimates (as produced
by the \code{metapop-cellular-automaton} vignette), build linear interpolation
functions for \code{T_E(S_frac)}, \code{T_P(S_frac)}, and \code{Q(S_frac)} at a chosen
coupling level \code{mu_r}. The returned lookup list is the form
\code{\link[=ca_step]{ca_step()}} expects.
}
\details{
Pads the empirical curves with \verb{(S_frac = 0, T_i = 0)} so fully-resistant
cells never trigger, and fills \code{Q_i} NAs (rare-trigger noise) with \code{1}.
}

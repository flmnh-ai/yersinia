% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnose_bound_piling}
\alias{diagnose_bound_piling}
\title{Detect posterior mass piling against prior bounds.}
\usage{
diagnose_bound_piling(samples, bounds, threshold = 0.2, edge = 0.05)
}
\arguments{
\item{samples}{A \code{monty_samples} object or a \code{posterior::draws_array}.}

\item{bounds}{Named list keyed by parameter name; each entry is a length-2
numeric \code{c(low, high)}. Parameters not in \code{bounds} are skipped.}

\item{threshold}{Fraction of draws within \code{edge} of a bound that triggers
the flag. Default 0.20 (20\%).}

\item{edge}{Fraction of bound width counted as "near the edge". Default
0.05 (5\%).}
}
\value{
A diagnostic record or \code{NULL}.
}
\description{
For each parameter with finite \verb{[low, high]} bounds in \code{bounds}, computes
the fraction of draws within \code{edge} (a fraction of the bound width) of
either bound. Flags any parameter where that fraction exceeds \code{threshold}.
Bound-piling indicates the prior is constraining the posterior — either
the prior is too tight, or the model wants a parameter the prior forbids.
}
\details{
Parameters with non-finite bounds (e.g. \code{Exponential(rate=0.1)} with
\code{high = Inf}) are silently skipped — there's no upper edge to pile against.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_model_config.R
\name{model_config_resolve}
\alias{model_config_resolve}
\title{Resolve a model config to per-parameter scope and value.}
\usage{
model_config_resolve(config)
}
\arguments{
\item{config}{A model-config list (see file header).}
}
\value{
A tibble with columns \code{parameter}, \code{scope}, \code{scenario_value},
\code{resolved_value}, sorted alphabetically.
}
\description{
Loads the named scenario, then for every configurable parameter reports
the scenario value, the chosen scope (\code{fixed | shared | local}), and the
value the model would actually see (\code{scenario_value} for \code{fixed}, \code{NA}
for \code{shared}/\code{local} since the sampler will set those).
}
\details{
Parameters that appear in both \code{shared} and \code{local} are reported as
\code{local} — the multi-cohort case usually wants per-outbreak resolution
when the user has flagged conflict.
}

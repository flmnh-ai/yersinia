% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/packer_helpers.R
\name{with_briere_seasonal}
\alias{with_briere_seasonal}
\title{Compute per-group seasonal forcing from temperature via Brière.}
\usage{
with_briere_seasonal(packer, group_temp, T_ref = 17.8, cap = 1000)
}
\arguments{
\item{packer}{A grouped packer or wrapper around one. Must have \code{T_min},
\code{T_max}, \code{q_briere} as shared scalar parameters.}

\item{group_temp}{Named list keyed by group; each entry is a numeric
per-day temperature vector (°C, length = number of simulation days).}

\item{T_ref}{Reference temperature at which the Brière multiplier equals
1 (i.e. the scenario \code{delta_R} is unchanged). Default 17.8°C.}

\item{cap}{Maximum allowed multiplier — clamps the transmission-off
signal when params are out of range. Default 1000.}
}
\value{
A packer with wrapped \verb{$unpack}.
}
\description{
For each group, transforms the temperature time series into a per-day
multiplier on \code{delta_R} (carcass decay) using the Brière function
parameterised by fitted \code{T_min}, \code{T_max}, \code{q_briere}. The multiplier is
normalised so \code{delta_R} equals its scenario value at \code{T_ref} (default
17.8°C, the typical mid-range plague-permissive temperature) — i.e.
calibrating outside the Brière transformation reaches the unmodulated
scenario \code{delta_R} at the reference temperature.
}
\details{
If \code{T_ref} falls outside \verb{[T_min, T_max]} (sampler exploring biologically
implausible cliffs), returns \code{cap} for all temperatures, which collapses
transmission and signals the proposal is bad. Per-temperature points
outside \verb{[T_min, T_max]} are also capped. Removes \code{T_min}, \code{T_max},
\code{q_briere} from unpacked pars.
}

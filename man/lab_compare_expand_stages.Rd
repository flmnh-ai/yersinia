% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_compare.R
\name{lab_compare_expand_stages}
\alias{lab_compare_expand_stages}
\title{Expand a selection so stochastic entries contribute both stages.}
\usage{
lab_compare_expand_stages(slugs, library_dir = NULL)
}
\arguments{
\item{slugs}{Character vector of library slugs.}

\item{library_dir}{Library directory. Defaults to \code{\link[=lab_library_dir]{lab_library_dir()}}.}
}
\value{
List with \code{slug} and \code{stage}, suitable for \code{\link[=lab_compare_draws]{lab_compare_draws()}}.
}
\description{
A stochastic run stores \code{pilot_samples} alongside the production draws;
nothing else in the app can show them. This turns each such entry into two
comparable series so a run can be checked against its own pilot.
}
\details{
Entries with no separate pilot (a deterministic fit, where the pilot \emph{is}
the canonical posterior) contribute one series, never a duplicate.
}

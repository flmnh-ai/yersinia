% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/packer_helpers.R
\name{with_R0_to_beta_r}
\alias{with_R0_to_beta_r}
\title{Convert R0 to beta_r per group during unpack.}
\usage{
with_R0_to_beta_r(packer)
}
\arguments{
\item{packer}{A grouped packer or wrapper around one. Must have \code{R0} as a
shared scalar parameter.}
}
\value{
A packer with wrapped \verb{$unpack}.
}
\description{
Sampling R0 directly (rather than beta_r) gives a more interpretable prior
and a flatter posterior geometry. The conversion uses the disease-free
equilibrium derivation in the carcass model (see CLAUDE.md "R0 formula"):
\code{beta_r = R0 * delta_R / ((1 - g_r) * (1 - exp(-rho)))}.
}
\details{
Requires \code{g_r}, \code{rho}, \code{delta_R} to already be present in each group's pars
(typically via the packer's global \code{fixed} argument). Removes \code{R0} from the
unpacked pars after conversion.
}

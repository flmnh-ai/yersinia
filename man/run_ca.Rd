% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ca.R
\name{run_ca}
\alias{run_ca}
\title{Run a CA from \code{n_seeds} seed cells for \code{n_ticks}}
\usage{
run_ca(lattice, lookups, d_per_tick, n_ticks, n_seeds = 5, seed = NULL, ...)
}
\arguments{
\item{lattice}{Lattice list from \code{\link[=make_square_grid]{make_square_grid()}} or
\code{\link[=make_hex_lattice]{make_hex_lattice()}}.}

\item{lookups}{Lookup list from \code{\link[=make_lookups]{make_lookups()}}.}

\item{d_per_tick}{\verb{P → S} decay probability per tick.}

\item{n_ticks}{Integer number of ticks to advance.}

\item{n_seeds}{Integer number of cells to seed in state \code{E} at tick 0.}

\item{seed}{Optional integer for \code{set.seed()} to make the run reproducible.}

\item{...}{Additional arguments forwarded to \code{\link[=ca_step]{ca_step()}} (e.g.
\code{P_recovery_sus}, \code{grow_per_tick}).}
}
\value{
Integer matrix of shape \verb{(n_ticks + 1, npop)} with row \code{t + 1}
the state at tick \code{t}.
}
\description{
Initializes a fresh lattice (all \code{S} with full susceptibility), seeds
\code{n_seeds} cells in state \code{E}, and advances \code{\link[=ca_step]{ca_step()}} for \code{n_ticks}.
Returns the per-tick state matrix; the \code{sus} history is not retained
(regenerable from \code{state} and the recovery rules if needed).
}

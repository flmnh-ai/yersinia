% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plague_utils.R
\name{run_plague_metapop_model}
\alias{run_plague_metapop_model}
\title{Run plague metapopulation model}
\usage{
run_plague_metapop_model(
  scenario = "defaults",
  npop,
  contact_r,
  mu_r,
  contact_h = NULL,
  mu_h = 0,
  K_r,
  K_h,
  I_ini,
  R_ini = NULL,
  I_h_ini = NULL,
  R_h_ini = NULL,
  years = 1,
  timestep = c("daily", "weekly"),
  n_particles = 100,
  n_threads = 1,
  obs_period = 1L,
  seasonal = NULL,
  ...
)
}
\arguments{
\item{scenario}{Parameter set name, file path, or list of parameters.}

\item{npop}{Integer number of patches.}

\item{contact_r}{Row-stochastic \verb{npop x npop} destination matrix
(\code{contact_r[i, j]} = probability that a rat leaving patch \code{i} arrives in
patch \code{j}). Must have zero diagonal.}

\item{mu_r}{Rat migration rate per day. Same value applies to S, I, R rats.}

\item{contact_h}{Row-stochastic \verb{npop x npop} destination matrix for
humans. Defaults to \code{contact_r} (assumes humans share the rat-transport
network) when \code{NULL}. Same validation rules as \code{contact_r}.}

\item{mu_h}{Human migration rate per day (default 0 -- sessile humans).
Same value applies to S_h, I_h, R_h.}

\item{K_r}{Length-\code{npop} vector of rat carrying capacities per patch.}

\item{K_h}{Length-\code{npop} vector of human carrying capacities per patch.}

\item{I_ini}{Length-\code{npop} vector of initial infected rats per patch.}

\item{R_ini}{Length-\code{npop} vector of initial heritably-resistant rats
per patch (default all zero).}

\item{I_h_ini}{Length-\code{npop} vector of initial infected humans per patch
(default all zero).}

\item{R_h_ini}{Length-\code{npop} vector of initial recovered humans per patch
(default all zero).}

\item{years}{Number of years to simulate (default 1).}

\item{timestep}{"weekly" or "daily" (default "daily" -- migration is
typically a daily-cadence process).}

\item{n_particles}{Number of particles for the stochastic run.}

\item{n_threads}{Number of threads for parallel processing.}

\item{obs_period}{Integer length (in days) of the observation window for
\code{D_h} / \code{D_r} (default 1L; requires \code{timestep = "daily"} if > 1).}

\item{seasonal}{Optional length-\code{n_days} per-day multiplier on carcass
decay (defaults to no seasonality).}

\item{...}{Additional scalar parameter overrides (\code{beta_r}, \code{rho}, ...).}
}
\value{
\code{plague_results} tibble with \code{population} column running 1..npop.
}
\description{
Forward-simulates the metapopulation plague model
(\code{inst/odin/plague_stochastic_metapop.R}). Same Didelot carcass-based
transmission core as \code{\link[=run_plague_model]{run_plague_model()}}, with rats dispersing between
patches at rate \code{mu_r} (via \code{contact_r}) and humans dispersing at rate
\code{mu_h} (via \code{contact_h}). For each species, susceptible, infected, and
recovered individuals migrate at the same rate; carcasses Q are sessile.
Human-side: I_h represents both incubating and symptomatic infections
(the model has no E_h compartment), and small \code{mu_h} is the parsimonious
way to express that humans are sedentary on outbreak timescales.
}
\details{
Per-patch parameters (\code{K_r}, \code{K_h}, \code{I_ini}, \code{R_ini}, \code{I_h_ini}, \code{R_h_ini})
must be length-\code{npop} numeric vectors. All transmission and demographic
rates are scalars shared across patches (treat them as biological
constants, not place-specific). For per-patch \code{beta_r[i]} etc., edit
\code{inst/odin/plague_stochastic_metapop.R} directly.
}

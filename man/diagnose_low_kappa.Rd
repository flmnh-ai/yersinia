% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnose_low_kappa}
\alias{diagnose_low_kappa}
\title{Detect low NegBinomial dispersion (\code{kappa}).}
\usage{
diagnose_low_kappa(samples, par = "kappa", threshold = 5)
}
\arguments{
\item{samples}{A \code{monty_samples} object or a \code{posterior::draws_array}.}

\item{par}{Name of the dispersion parameter. Default \code{"kappa"}.}

\item{threshold}{Median \code{kappa} below which the fit is flagged. Default 5.}
}
\value{
A diagnostic record or \code{NULL} (also \code{NULL} if \code{par} isn't in the
sampled parameters — kappa may be pinned in some configurations).
}
\description{
The plague-fit likelihood is \code{deaths ~ NegBinomial(size = kappa, mu = ...)}.
Small \code{kappa} means highly overdispersed observations — the data is so
noisy that almost any trajectory is plausible, so the posterior over
transmission parameters becomes uninformative. The MVP threshold of 5 is
a rule of thumb from the Cairo / Barcelona pilots in this conversation:
kappa < 5 typically corresponded to broken or under-specified fits.
}
\details{
Uses the posterior median of \code{kappa} across all chains and iterations.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnose_chain_stuck}
\alias{diagnose_chain_stuck}
\title{Detect non-mixing chains via R-hat.}
\usage{
diagnose_chain_stuck(samples, threshold = 1.5)
}
\arguments{
\item{samples}{A \code{monty_samples} object or a \code{posterior::draws_array}.}

\item{threshold}{R-hat above which the fit is flagged. Default 1.5.}
}
\value{
A diagnostic record (see file header) or \code{NULL} if all R-hats are
below \code{threshold}.
}
\description{
Uses \code{\link[posterior:rhat]{posterior::rhat()}} on each parameter; flags the fit if any parameter's
R-hat exceeds \code{threshold}. The MVP threshold of 1.5 is intentionally loose
(the conventional convergence cutoff is 1.05–1.1) — we want to flag pilots
that are obviously broken, not over-warn on borderline cases.
}

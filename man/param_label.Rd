% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_param_labels.R
\name{param_label}
\alias{param_label}
\title{Human-readable label for a model parameter.}
\usage{
param_label(x, with_symbol = TRUE)
}
\arguments{
\item{x}{Character vector of parameter names.}

\item{with_symbol}{Include the symbol in parentheses. Default \code{TRUE}.}
}
\value{
Character vector of labels, same length as \code{x}.
}
\description{
Renders \code{beta_h} as \code{"Carcass-to-human transmission (beta_h)"}, with the
symbol in Greek where the literature uses Greek. Grouped parameter names
from \code{\link[monty:monty_packer_grouped]{monty::monty_packer_grouped()}} (\verb{param<group>}) keep their group,
appended after a separator, so per-outbreak panels stay distinguishable.
}
\details{
Unknown names pass through unchanged -- a parameter added to the model still
plots, it just isn't prettified yet.
}
\examples{
param_label(c("beta_h", "kappa", "beta_h<Eyam_1665>"))
}
